import 'package:flutter/material.dart';
import '../placeholder_screen.dart';

class BinInventoryScreen extends StatelessWidget {
  const BinInventoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(title: 'Bin Inventory');
  }
}
